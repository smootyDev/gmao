# Copia este fichero a .env antes de levantar la aplicación:
#   cp .env.example .env
#
# El valor de abajo funciona tal cual para probar en local, pero es público
# (está en el repositorio): antes de un despliegue real, genera el tuyo con
#   openssl rand -base64 32
# o
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# y sustitúyelo aquí.
JWT_SECRET=wdnKIPY7LxULcvqqX8JAzSU4a/C7ATWwyqstb021TPI=
